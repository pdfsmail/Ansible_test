# Ansible_test

My testing with ansible.

I have noticed that for workstations, using Kubuntu tends to throw errors when using ansible to update or 
install files where the regular gnome version does not have issues with it.
